@using ProjetoFim.Models
@using ProjetoFim.Resources
@using System.Globalization
@{
    var utilizador = (ApplicationUser)ViewBag.Utilizador;
    var quarto = (Quarto)ViewBag.Quarto;
    var dataInicio = (DateTime)ViewBag.DataInicio;
    var dataFim = (DateTime)ViewBag.DataFim;
    var numHospedes = (int)ViewBag.NumHospedes;
    var numeroNoites = (int)ViewBag.NumeroNoites;
    var precoTotal = (decimal)ViewBag.PrecoTotal;
    var culturaEuro = new CultureInfo("pt-PT");

    ViewBag.Title = Strings.PaginaReserva_finalizar_reserva;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="booking-page-wrapper">
    @using (Html.BeginForm("Criar", "Reserva", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        <input type="hidden" name="quartoId" value="@quarto.Id" />
        <input type="hidden" name="dataInicio" value="@dataInicio.ToString("yyyy-MM-dd")" />
        <input type="hidden" name="dataFim" value="@dataFim.ToString("yyyy-MM-dd")" />

        <div class="booking-page-container">
            <main class="booking-form-column">
                <div class="form-section">
                    <h2>@Strings.PaginaPagamento_como_gostaria_de_pagar</h2>
                    <p>@Strings.PaginaPagamento_preencha_os_seus_dados_e_os_detalhes_de_</p>
                </div>
                <div class="form-section">
                    <h4>@Strings.PaginaPagamento_os_seus_dados</h4>
                    <div class="booking-form">
                        <div class="form-group">
                            <label>@Strings.PaginaPagamento_nome_completo</label>
                            <input type="text" value="@utilizador.UserName" required />
                        </div>
                        <div class="form-group">
                            <label>Email</label>
                            <input type="email" value="@utilizador.Email" required />
                        </div>
                    </div>
                </div>
                <div class="form-section">
                    <h4>@Strings.PaginaPagamento_detalhes_do_pagamento</h4>
                    <p>@Strings.PaginaPagamento_funcionalidade_pagamento_futuro</p>
                </div>
                <div class="form-actions-final">
                    <button type="submit" class="submit-booking-btn primary">@Strings.PaginaPagamento_concluir_reserva</button>
                </div>
            </main>
            <aside class="booking-summary-column">
                <div class="booking-summary-box">
                    <div class="room-choice-summary">
                        <div class="summary-image" style="background-image: url('@Url.Content(quarto.Imagens.OrderBy(i => i.Ordem).FirstOrDefault()?.Url ?? "/Content/placeholder.jpg")');"></div>
                        <div>
                            <h4>@quarto.Nome</h4>
                            <p>@quarto.Localizacao</p>
                            <p class="rating"><i class="fas fa-star"></i> @quarto.AvaliacaoMedia.ToString("F1")</p>
                        </div>
                    </div>
                    <hr />
                    <h4>@Strings.PaginaPagamento_sum_rio_do_pre_o</h4>
                    <ul class="price-summary-list">
                        <li class="price-item">
                            <span>@precoTotal.ToString("C", culturaEuro) x @numeroNoites @Strings.Reservas_preco</span>
                            <span>@precoTotal.ToString("C", culturaEuro)</span>
                        </li>
                        <li class="price-item">
                            <span>@Strings.PaginaPagamento_impostos_e_taxas</span>
                            <span>@Strings.PaginaPagamento_a_calcular</span>
                        </li>
                    </ul>
                    <div class="price-total">
                        <span>@Strings.PaginaPagamento_pre_o_total</span>
                        <strong>@precoTotal.ToString("C", culturaEuro)</strong>
                    </div>
                </div>
            </aside>
        </div>
    }
</div>
